**Architettura Web e Flask**
- A cosa serve il file `__init__.py` all'interno della cartella `app`?

Un file __init__.py all'interno della cartella "app" serve per inizializzare le sottocartelle con all'interno i loro file come eseguibili. Può essere sia vuoto di contenuto che con import dei vari file del progetto. Senza di esso le cartelle non vengono gestite come codice eseguibile. In un progetto va messo per ogni sopttocartella dove si desidera avere il codice eseguibile.